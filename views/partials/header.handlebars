        <!-- шапка -->
        <header class="blog-header lh-1 py-3">
          <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1">
              {{#if reg}}
                {{reg}}
              {{/if}}
            </div>
            <div class="col-4 text-center">
              <a class="blog-header-logo text-dark" href="/home">CHAT</a>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
              {{#if reg}}
                <a class="btn btn-sm btn-outline-secondary" href="/users">Выйти</a>
                {{else}}
                <a class="btn btn-sm btn-outline-secondary" href="/sign-in">Войти</a>
              {{/if}}   
            </div>
          </div>
        </header>

        <!-- список чатов -->
        <div class="nav-scroller py-1 mb-2">
          <nav class="nav d-flex justify-content-between" id="RoomsNav">
            {{!-- {{{AllRooms}}} --}}
            {{#each AllRooms}}
                <a class="p-2 link-secondary" href={{urlRooms}}>{{name}}{{url_rooms}}</a>
            {{/each}}
          </nav>
        </div>
      </div>